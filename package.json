{
  "name": "mobility-server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "prebuild": "eslint . --ext .ts",
    "lint": "eslint . --ext .ts --fix",
    "build": "tsc",
    "start": "tsc && node .",
    "dev": "ts-node src/app.ts",
    "dev2": "concurrently \"tsc --watch\" \"nodemon -q dist/app.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.13.0",
    "express": "^4.18.2"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.1.0",
    "concurrently": "^8.0.1",
    "eslint": "^8.40.0",
    "nodemon": "^2.0.22",
    "prisma": "^4.13.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
